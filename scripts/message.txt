feat(WebCamRecordFaceDetection): Adding metrics and changing layout

Now the layout is using two columns to define
better the layout. It was added metrics that show 
if it's been detect a face or not.